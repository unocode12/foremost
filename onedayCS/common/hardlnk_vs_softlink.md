# 하드링크(Hard Link) vs 소프트링크(Soft Link / Symbolic Link)

## 📋 목차

1. [개요](#1-개요)
2. [하드링크 (Hard Link)](#2-하드링크-hard-link)
3. [소프트링크 (Soft Link / Symbolic Link)](#3-소프트링크-soft-link--symbolic-link)
4. [비교 분석](#4-비교-분석)
5. [실전 사용 예시](#5-실전-사용-예시)
6. [핵심 정리](#6-핵심-정리)

---

## 1. 개요

### 1.1 Linux 파일 시스템 구조

Linux 파일 시스템에서 파일은 **inode (Index Node)**와 **데이터 블록**으로 구성됩니다.

**inode 구조**:
- 파일 메타데이터 (권한, 소유자, 크기, 타임스탬프 등)
- 데이터 블록 포인터
- 링크 카운터 (하드링크 개수)

**파일 시스템 구조**:
```
[Directory Entry] → [inode] → [Data Blocks]
     (파일명)      (메타데이터)   (실제 데이터)
```

---

### 1.2 링크의 개념

**링크 (Link)**는 같은 파일이나 디렉토리를 가리키는 여러 경로를 만드는 메커니즘입니다.

**링크의 종류**:
- **하드링크 (Hard Link)**: 같은 inode를 가리키는 여러 디렉토리 엔트리
- **소프트링크 (Symbolic Link)**: 다른 파일을 가리키는 특수 파일

---

## 2. 하드링크 (Hard Link)

### 2.1 정의

**하드링크**는 같은 inode를 가리키는 여러 디렉토리 엔트리입니다. 모든 하드링크는 동일한 파일을 참조하며, 원본 파일과 구분할 수 없습니다.

**핵심 특징**:
- 같은 inode 번호를 가짐
- 원본 파일과 동일한 파일
- 디렉토리 간 이동 가능 (같은 파일 시스템 내)
- 원본 파일 삭제해도 다른 하드링크는 유지

---

### 2.2 inode와의 관계

**하드링크 생성 과정**:
```
1. 원본 파일: /home/user/file.txt (inode: 12345)
2. 하드링크 생성: ln /home/user/file.txt /home/user/link.txt
3. 결과:
   - /home/user/file.txt → inode 12345
   - /home/user/link.txt → inode 12345 (같은 inode!)
   - inode 12345의 링크 카운터: 2
```

**inode 링크 카운터**:
- 하드링크가 생성될 때마다 증가
- 하드링크가 삭제될 때마다 감소
- 링크 카운터가 0이 되면 파일이 실제로 삭제됨

---

### 2.3 하드링크 생성 및 확인

**생성 명령어**:
```bash
# 기본 문법
ln [원본 파일] [링크 이름]

# 예시
ln /home/user/document.txt /home/user/doc.txt
ln /var/log/messages /tmp/messages_link
```

**하드링크 확인**:
```bash
# inode 번호 확인 (같은 inode = 하드링크)
ls -li

# 출력 예시:
# 12345 -rw-r--r-- 2 user user 1024 Jan 15 10:00 file.txt
# 12345 -rw-r--r-- 2 user user 1024 Jan 15 10:00 link.txt
# ↑ 같은 inode 번호 (12345)
#        ↑ 링크 카운터 (2개)

# stat 명령으로 상세 정보 확인
stat file.txt
stat link.txt
# 둘 다 같은 inode 번호와 링크 카운터 표시
```

**링크 카운터 확인**:
```bash
# ls -l의 두 번째 필드가 링크 카운터
ls -l file.txt
# -rw-r--r-- 2 user user 1024 Jan 15 10:00 file.txt
#            ↑ 링크 카운터 (2개)

# find 명령으로 같은 inode 찾기
find /home -inum 12345
```

---

### 2.4 하드링크의 제약사항

**제약사항**:
1. **같은 파일 시스템 내에서만 가능**
   - 다른 파일 시스템 간 하드링크 불가
   - 마운트 포인트를 넘어서는 하드링크 불가

2. **디렉토리에 대한 하드링크 불가**
   - 파일에만 하드링크 생성 가능
   - 디렉토리 하드링크는 시스템 레벨에서만 가능 (`.`, `..`)

3. **원본과 구분 불가**
   - 어떤 것이 원본인지 알 수 없음
   - 모든 하드링크가 동등함

---

### 2.5 하드링크 삭제

**삭제 동작**:
```bash
# 하드링크 삭제
rm link.txt

# 결과:
# - inode의 링크 카운터가 1 감소 (2 → 1)
# - file.txt는 여전히 존재 (데이터 유지)
# - link.txt만 삭제됨

# 모든 하드링크 삭제 시
rm file.txt
# - inode의 링크 카운터가 0이 됨
# - 실제 파일 데이터가 삭제됨
```

**링크 카운터 동작**:
```
원본 파일 생성: 링크 카운터 = 1
하드링크 생성: 링크 카운터 = 2
하드링크 삭제: 링크 카운터 = 1 (파일 유지)
원본 삭제: 링크 카운터 = 0 (파일 삭제)
```

---

## 3. 소프트링크 (Soft Link / Symbolic Link)

### 3.1 정의

**소프트링크 (심볼릭 링크)**는 다른 파일이나 디렉토리를 가리키는 특수 파일입니다. 원본 파일의 경로를 저장하는 별도의 파일입니다.

**핵심 특징**:
- 별도의 inode를 가짐
- 원본 파일의 경로를 저장
- 원본 파일 삭제 시 깨진 링크 (broken link)
- 다른 파일 시스템 간 링크 가능
- 디렉토리 링크 가능

---

### 3.2 심볼릭 링크 구조

**심볼릭 링크 생성 과정**:
```
1. 원본 파일: /home/user/file.txt (inode: 12345)
2. 심볼릭 링크 생성: ln -s /home/user/file.txt /home/user/symlink.txt
3. 결과:
   - /home/user/file.txt → inode 12345 (원본)
   - /home/user/symlink.txt → inode 67890 (새로운 inode)
   - inode 67890의 데이터: "/home/user/file.txt" (경로 문자열)
```

**심볼릭 링크 파일 구조**:
```
[symlink.txt] (inode: 67890)
    ↓
[데이터: "/home/user/file.txt"] (경로 문자열)
    ↓
[실제 파일] (inode: 12345)
```

---

### 3.3 심볼릭 링크 생성 및 확인

**생성 명령어**:
```bash
# 기본 문법
ln -s [원본 파일/디렉토리] [링크 이름]

# 예시
ln -s /home/user/document.txt /home/user/doc.txt
ln -s /var/log /tmp/log_link
ln -s /usr/bin/python3 /usr/local/bin/python
```

**심볼릭 링크 확인**:
```bash
# ls -l로 확인 (화살표 표시)
ls -l

# 출력 예시:
# 12345 -rw-r--r-- 1 user user 1024 Jan 15 10:00 file.txt
# 67890 lrwxrwxrwx 1 user user   20 Jan 15 10:01 symlink.txt -> file.txt
# ↑ 다른 inode 번호
#        ↑ 'l'로 시작 (링크 파일)
#                    ↑ 화살표로 원본 표시

# readlink 명령으로 원본 경로 확인
readlink symlink.txt
# 출력: file.txt

# 절대 경로 확인
readlink -f symlink.txt
# 출력: /home/user/file.txt

# file 명령으로 확인
file symlink.txt
# 출력: symlink.txt: symbolic link to file.txt
```

**깨진 링크 확인**:
```bash
# 깨진 링크 확인
ls -l broken_link
# 출력: lrwxrwxrwx 1 user user 20 Jan 15 10:01 broken_link -> non_existent_file

# file 명령으로 확인
file broken_link
# 출력: broken_link: broken symbolic link to non_existent_file

# find 명령으로 깨진 링크 찾기
find /home -type l ! -exec test -e {} \; -print
```

---

### 3.4 심볼릭 링크의 장점

**장점**:
1. **다른 파일 시스템 간 링크 가능**
   - 마운트 포인트를 넘어서는 링크 가능
   - 네트워크 파일 시스템에도 링크 가능

2. **디렉토리 링크 가능**
   - 디렉토리에 대한 심볼릭 링크 생성 가능
   - 유용한 디렉토리 구조 생성

3. **원본과 구분 가능**
   - 원본 파일과 링크 파일 구분 가능
   - 원본 파일 경로 확인 가능

4. **동적 경로 변경**
   - 링크를 다시 생성하여 다른 파일을 가리키도록 변경 가능

---

### 3.5 심볼릭 링크의 단점

**단점**:
1. **원본 파일 삭제 시 깨짐**
   - 원본 파일 삭제 시 깨진 링크가 됨
   - 깨진 링크는 사용 불가

2. **성능 오버헤드**
   - 원본 파일 접근 시 경로 해석 필요
   - 추가적인 파일 시스템 접근

3. **순환 참조 가능**
   - A → B → A 같은 순환 참조 가능
   - 무한 루프 위험

---

### 3.6 심볼릭 링크 삭제

**삭제 동작**:
```bash
# 심볼릭 링크 삭제
rm symlink.txt

# 결과:
# - 심볼릭 링크 파일만 삭제됨
# - 원본 파일은 영향 없음
# - 원본 파일의 링크 카운터 변화 없음
```

**원본 파일 삭제 시**:
```bash
# 원본 파일 삭제
rm file.txt

# 결과:
# - 원본 파일 삭제됨
# - 심볼릭 링크는 깨진 링크가 됨
# - symlink.txt는 존재하지만 작동하지 않음
```

---

## 4. 비교 분석

### 4.1 상세 비교표

| 항목 | 하드링크 (Hard Link) | 소프트링크 (Symbolic Link) |
|------|---------------------|---------------------------|
| **inode** | 원본과 같은 inode | 별도의 inode |
| **파일 타입** | 일반 파일과 동일 | 특수 파일 (링크 파일) |
| **링크 카운터** | 증가함 | 영향 없음 |
| **원본 삭제 시** | 다른 링크 유지 | 깨진 링크 |
| **파일 시스템** | 같은 파일 시스템만 | 다른 파일 시스템 가능 |
| **디렉토리** | 불가능 | 가능 |
| **원본 구분** | 불가능 | 가능 (경로 확인) |
| **성능** | 빠름 (직접 접근) | 약간 느림 (경로 해석) |
| **크기** | 원본과 동일 | 경로 문자열 크기 |
| **권한** | 원본과 동일 | 항상 777 (실제 권한은 원본) |

---

### 4.2 inode 관점 비교

**하드링크**:
```
file1.txt (inode: 12345) ─┐
                           ├─→ [inode 12345] → [Data Blocks]
file2.txt (inode: 12345) ─┘
```

**소프트링크**:
```
file.txt (inode: 12345) ──→ [inode 12345] → [Data Blocks]

symlink.txt (inode: 67890) ──→ [inode 67890] → ["file.txt"]
                                      ↓
                              [inode 12345] → [Data Blocks]
```

---

### 4.3 파일 시스템 레벨 비교

**하드링크**:
- 디렉토리 엔트리만 추가
- 같은 inode 참조
- 링크 카운터 증가

**소프트링크**:
- 새로운 inode 생성
- 경로 문자열 저장
- 원본 파일 경로 해석 필요

---

## 5. 실전 사용 예시

### 5.1 하드링크 사용 예시

**예시 1: 백업 파일 관리**
```bash
# 원본 파일
touch important.txt

# 여러 위치에 하드링크 생성
ln important.txt /backup/important.txt
ln important.txt /archive/important.txt

# 어느 것을 수정해도 모두 동일하게 변경됨
echo "update" >> important.txt
# /backup/important.txt와 /archive/important.txt도 동일하게 변경됨

# 하나 삭제해도 다른 링크는 유지
rm important.txt
# /backup/important.txt와 /archive/important.txt는 여전히 존재
```

**예시 2: 디스크 공간 절약**
```bash
# 같은 파일을 여러 위치에서 참조
ln /usr/share/common/data.txt /home/user1/data.txt
ln /usr/share/common/data.txt /home/user2/data.txt
ln /usr/share/common/data.txt /home/user3/data.txt

# 디스크 공간은 1개 파일만 사용
du -sh /usr/share/common/data.txt
# 출력: 1.0M

# 각 하드링크는 추가 공간 사용 안 함
```

---

### 5.2 심볼릭 링크 사용 예시

**예시 1: 버전 관리**
```bash
# Python 버전 관리
ln -s /usr/bin/python3.9 /usr/local/bin/python
ln -s /usr/bin/python3.10 /usr/local/bin/python3.10

# 버전 변경 시 링크만 재생성
rm /usr/local/bin/python
ln -s /usr/bin/python3.10 /usr/local/bin/python
```

**예시 2: 디렉토리 구조 단순화**
```bash
# 긴 경로를 짧은 이름으로
ln -s /var/log/apache2/access.log /tmp/apache.log
ln -s /home/user/documents/projects /home/user/proj

# 사용
cat /tmp/apache.log
cd /home/user/proj
```

**예시 3: 설정 파일 관리**
```bash
# 설정 파일을 다른 위치로 이동 후 링크
mv /etc/nginx/nginx.conf /home/user/configs/nginx.conf
ln -s /home/user/configs/nginx.conf /etc/nginx/nginx.conf

# 원본은 다른 위치에 있지만 원래 경로에서 접근 가능
```

**예시 4: 공유 라이브러리 관리**
```bash
# 라이브러리 버전 관리
ln -s libmylib.so.1.0.0 libmylib.so.1
ln -s libmylib.so.1 libmylib.so

# 버전 업그레이드 시
rm libmylib.so.1.0.0
ln -s libmylib.so.1.1.0 libmylib.so.1.0.0
```

---

### 5.3 실무 활용 패턴

**패턴 1: 로그 파일 관리**
```bash
# 로그 파일을 다른 위치로 이동 후 링크
mv /var/log/app.log /mnt/storage/logs/app.log
ln -s /mnt/storage/logs/app.log /var/log/app.log

# 애플리케이션은 원래 경로 사용 가능
tail -f /var/log/app.log
```

**패턴 2: 홈 디렉토리 정리**
```bash
# 큰 파일을 다른 위치로 이동
mv ~/Downloads/large_file.zip /mnt/storage/large_file.zip
ln -s /mnt/storage/large_file.zip ~/Downloads/large_file.zip

# 홈 디렉토리 공간 절약
```

**패턴 3: 개발 환경 설정**
```bash
# 프로젝트 설정 파일 링크
ln -s ~/dotfiles/.vimrc ~/.vimrc
ln -s ~/dotfiles/.bashrc ~/.bashrc
ln -s ~/dotfiles/.gitconfig ~/.gitconfig

# 설정 파일을 한 곳에서 관리
```

---

## 6. 핵심 정리

### 6.1 선택 가이드

**하드링크를 사용하는 경우**:
- ✅ 같은 파일 시스템 내에서만 링크 필요
- ✅ 원본 파일 삭제 후에도 링크 유지 필요
- ✅ 디스크 공간 절약이 중요한 경우
- ✅ 성능이 중요한 경우

**심볼릭 링크를 사용하는 경우**:
- ✅ 다른 파일 시스템 간 링크 필요
- ✅ 디렉토리 링크 필요
- ✅ 원본과 링크를 구분해야 하는 경우
- ✅ 동적으로 링크 대상을 변경해야 하는 경우

---

### 6.2 주의사항

**하드링크 주의사항**:
- ⚠️ 같은 파일 시스템 내에서만 가능
- ⚠️ 디렉토리 하드링크 불가
- ⚠️ 원본과 구분 불가
- ⚠️ 순환 참조 체크 어려움

**심볼릭 링크 주의사항**:
- ⚠️ 원본 파일 삭제 시 깨짐
- ⚠️ 순환 참조 가능
- ⚠️ 상대 경로 사용 시 주의 필요
- ⚠️ 성능 오버헤드

---

### 6.3 명령어 요약

**하드링크**:
```bash
# 생성
ln source.txt link.txt

# 확인
ls -li
stat source.txt

# 삭제
rm link.txt
```

**심볼릭 링크**:
```bash
# 생성
ln -s source.txt symlink.txt

# 확인
ls -l
readlink symlink.txt
file symlink.txt

# 삭제
rm symlink.txt
```

---

### 6.4 디버깅 팁

**하드링크 디버깅**:
```bash
# 같은 inode를 가진 모든 파일 찾기
find /path -inum $(stat -c %i file.txt)

# 링크 카운터 확인
ls -l file.txt
# 두 번째 필드가 링크 카운터
```

**심볼릭 링크 디버깅**:
```bash
# 깨진 링크 찾기
find /path -type l ! -exec test -e {} \; -print

# 모든 심볼릭 링크 찾기
find /path -type l

# 원본 경로 확인
readlink -f symlink.txt
```

---

## 📚 참고 자료

- [Linux Filesystem Hierarchy Standard](https://refspecs.linuxfoundation.org/FHS_3.0/fhs/index.html)
- [GNU Coreutils: ln](https://www.gnu.org/software/coreutils/manual/html_node/ln-invocation.html)
- [Understanding Linux File Systems: inodes and Block Storage](https://opensource.com/article/19/5/how-linux-filesystem-works)

---

**작성일**: 2024  
**버전**: 1.0
